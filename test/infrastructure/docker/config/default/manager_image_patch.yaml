apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: public.ecr.aws/t0n3a9y4/kubernetes-sigs/cluster-api/capd-manager-amd64:dev
        name: manager
